# Copy this file to terraform.tfvars and fill in the values.
# NEVER commit terraform.tfvars to version control.

project_id     = "erdem-personal-website-prod"
project_number = "157799560453"
region         = "europe-west1"
service_name   = "personal-website"

# Secrets (MAPS_API_KEY, SECRET_KEY, GMAIL_APP_PASSWORD) are managed
# exclusively via GCP Secret Manager — never stored here or in Cloud Run env vars.
